I = imread('../test.jpg');

red = I(:,:,1);
green = I(:,:,2);
blue = I(:,:,3);



intensity = mean(I, 3);

boolean = abs(red - green) > dt;
boolean = boolean || abs(red - blue) > dt;
boolean = boolean || abs(blue - green) > dt;


